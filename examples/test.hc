#include <stdio.h>

int main()
    int x = 10
    int y = 20
    if (x > 5)
        x++
    else
        x--
    for (int i = 0; i < 5; i++)
        printf("%d", i)
    while (x < y)
        x++
    switch (x)
        case 1
            break
        default
            break
    return 0